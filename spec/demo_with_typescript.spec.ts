

describe('writing specs in typescript', () => {
    it('is easy', () => {
        expect(true).toBe(true);
    });
    it('counting letters', () => {
        const name = 'Waldo';
        expect(name.length).toBe(5);
    })
});

describe('writing more specs', () => {

    it('no problem', () => {
        expect(2 + 2).toBe(4);
    });
});
